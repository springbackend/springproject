<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b">
	<!-- <select id="board_list" resultType="board">
		SELECT * FROM board
		ORDER BY b_idx DESC
	</select> -->
	
	<select id="board_list" resultType="board" parameterType="java.util.HashMap">
		SELECT * FROM (SELECT RANK() OVER( ORDER BY b_idx DESC) no, b.*
	         		   FROM board b )
		WHERE no BETWEEN #{start} AND #{end}
	</select>
	
	<select id="board_count" resultType="int">
		SELECT COUNT(*) FROM board
	</select>
	
	<select id="board_one" resultType="board" parameterType="int">
		SELECT * FROM board
		WHERE b_idx = #{b_idx}
	</select>
	
	<insert id="board_write" parameterType="board">
		INSERT INTO board VALUES(
			seq_board_idx.nextVal,
			#{m_idx},
			#{subject},
			#{content},
			#{ip},
			sysdate,
			0,
			0,
			#{t_idx},
			#{p_idx},
			#{t_name},
			#{p_name}
		)
	</insert>
	
	<update id="increase_thumb" parameterType="int">
		UPDATE board SET recommend = recommend + 1
		WHERE b_idx = #{b_idx}
	</update>
	
	<update id="decrease_thumb" parameterType="int">
		UPDATE board SET recommend = recommend - 1
		WHERE b_idx = #{b_idx}
	</update>
</mapper>













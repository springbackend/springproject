<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="u">
	<!-- create -->
	<insert id="insert_user" parameterType="user">
		INSERT INTO USER1
		VALUES(
		USER_SEQ.nextVal,
		#{id},
		#{name},
		#{pwd},
		#{pnum},
		#{email},
		#{gender},
		#{regidate},
		#{regiip},
		#{addr},
		#{birth}
		)
	</insert>
	<!-- read -->
	<!-- read user all -->
	<select id="select_list" resultType="user">
		SELECT * FROM USER1
	</select>
	<!-- read user one -->
	<select id="ismember" resultType="user" parameterType="String">
		SELECT *
		FROM USER1
		WHERE u_id = '${value}'
	</select>
	<!-- update -->
	<!-- update pwd -->
	<update id="insert" parameterType="user">
		UPDATE USER1
		set
		pwd=#{pwd},
		WHERE id=#{id}
	</update>
	<!-- delete -->
	<delete id="delete" parameterType="String">
		delete 
		FROM USER1
		 WHERE u_id = #{id}
	</delete>
</mapper>











<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pc">
	<select id="product_comment" resultType="productcomment"
		parameterType="int">
		SELECT pc.pc_content, pc.pc_image, pc.pc_good, u.u_id,pc_star,pc_regidate
		FROM product_comment pc
		JOIN user1 u ON pc.u_idx = u.u_idx
		WHERE pc.p_idx = 1
	</select>
	
	<insert id="product_comment_write" parameterType="productcomment">
		INSERT INTO product_comment VALUES(pc_seq_idx.nextval,#{pc_content},#{pc_image},#{pc_good},#{p_idx},#{u_idx},#{pc_star},sysdate)
	</insert>
</mapper>






















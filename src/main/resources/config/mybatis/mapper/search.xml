<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="s">
	<select id="search_p_name" resultType="product" parameterType="java.util.Map">
			SELECT * FROM product WHERE p_name BETWEEN #{one} AND #{two}
	</select>
	<select id="search_p_namet" resultType="String" parameterType="String">
			SELECT p_name FROM product WHERE p_name like '%' || #{keyword} || '%'
	</select>
	
	<select id="search_result" resultType="String">
		select p_name from product
	</select>
	
	<select id="select_search_list" resultType="product" parameterType="java.util.Map">
		select * from (select rank() over(order by p_idx desc) no, p.* from product p where p.p_name like '%' || #{keyword} || '%') 
		where no between #{start} and #{end}
	</select>
	
	<select id="search_count" resultType="int" parameterType="String">
		select count(*) from product where p_name like '%' || #{keyword} || '%'
	</select>
</mapper>





















